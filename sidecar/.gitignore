# Trained checkpoints and weights (may encode a personal corpus -- never commit)
*.pt
*.pth
*.ckpt
*.npz
*.safetensors

# Personal/thought corpora -- never commit real corpora; the sample is the only exception
*.jsonl
!data/sample_corpus.jsonl

# Corpora / raw personal data directories
corpora/
checkpoints/*
!checkpoints/.gitkeep
*.jsonl.gz

# Secrets
.env
.env.*
keys/
*.key
*_token
*token.txt

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/

# run artifacts (generated by experiment scripts)
code/results/
code/checkpoints/*.pt
